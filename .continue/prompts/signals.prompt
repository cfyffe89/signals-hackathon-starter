---
name: signals
description: Write Signals API code for this repo (FastAPI/Streamlit), grounded in docs/signals
invokable: true
---
Follow AGENTS.md. Before writing code:
1. Use an existing method in backend/signals_client.py if one fits (see docs/signals/CHEAT_SHEET.md).
2. Otherwise confirm the endpoint exists: python docs/signals/scripts/lookup_endpoint.py --system core --grep <word>, then read its parameters with the lookup script.
3. Check docs/signals/GOTCHAS.md for traps on that endpoint (media types, keyword mode, ancestors + digest, source=IVT, ...).
Write FastAPI routes in backend/app.py or Streamlit UI in app_streamlit.py. Add new API calls as methods on SignalsClient.

User request:
{{{ input }}}
